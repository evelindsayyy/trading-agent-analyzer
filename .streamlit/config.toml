# Streamlit app config (committed). Secrets go in .streamlit/secrets.toml (ignored).
# Palette from the 智析 design: warm cream + deep teal.
[theme]
base = "light"
primaryColor = "#1C6E66"
backgroundColor = "#FBFAF6"
secondaryBackgroundColor = "#F4F2EA"
textColor = "#23211C"
font = "sans serif"

[server]
maxUploadSize = 5
