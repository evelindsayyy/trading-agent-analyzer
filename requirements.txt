# Web app dependency.
streamlit>=1.40

# This app imports the `tradingagents` package, which is a separate
# (open-source, locally-modified) project — install it editable into the SAME
# Python environment, e.g.:
#     uv pip install -e /path/to/TradingAgents
# It is intentionally NOT pinned here because it is not published to PyPI.
